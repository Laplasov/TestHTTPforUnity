{
  "name": "netlify-http-server",
  "version": "1.0.0",
  "description": "Simple HTTP server on Netlify",
  "scripts": {
    "start": "netlify dev"
  },
  "dependencies": {},
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
